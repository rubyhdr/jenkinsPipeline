[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: fast tests of pure business rules and services
    integration: tests that exercise the Flask app through its HTTP interface
    e2e: browser tests run with Selenium against a deployed environment
filterwarnings =
    ignore::DeprecationWarning:flask_login.*
