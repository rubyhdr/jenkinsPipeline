# Accepted image vulnerabilities for the security gate (trivy image --ignorefile .trivyignore).
# Each entry must be justified in docs/SECURITY_FINDINGS.md and reviewed when the base image changes.
# Currently empty: every fixable HIGH/CRITICAL finding has been remediated.
#
# IaC exceptions are made inline where they apply (see `# trivy:ignore:` comments in infra/*/Dockerfile).
